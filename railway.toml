[build]
builder = "dockerfile"

[deploy]
# Temporarily disable health check to debug SSL cert issue
# healthcheckPath = "/up"
healthcheckTimeout = 300
numReplicas = 1

[environments.production.variables]
RAILS_ENV = "production"
PORT = "3000"
