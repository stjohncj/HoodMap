<%= render 'info_pages/page_header', page_title: page_title %>

<div class="info-page-content">
  <% if defined?(images) && images %>
    <%= render 'shared/street_gallery', images: images %>
  <% end %>
  <%= render 'info_pages/body', page_title: page_title, content: content %>
</div>

<%= render 'shared/styles' %>